# Gym Companion

Local-first PWA gym tracker: workouts + rest timer, food log, weight trend. All data lives in
IndexedDB on the device — this repo is only the app shell. Backup/sync is a manual JSON
export/import from the More tab.

Install: open the GitHub Pages URL in Chrome on Android → menu → **Add to Home screen**.
Works fully offline after the first load.
